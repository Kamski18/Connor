{
  "build": {
    "commands": [
      "apt-get update",
      "apt-get install -y libgl1-mesa-glx",  // Install OpenGL dependency
      "pip install -r requirements.txt"      // Install Python dependencies
    ]
  },
  "start": {
    "command": "python main.py"
  }
}
